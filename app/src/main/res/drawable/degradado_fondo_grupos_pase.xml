<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo con gradiente -->
    <gradient
        android:startColor="@color/azul_tres"
        android:endColor="@color/azul_dos"
        android:angle="45" />

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" />

    <!-- Sombra suave -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp"/>
</shape>
